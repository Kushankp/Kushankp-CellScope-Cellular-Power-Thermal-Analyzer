# CellScope

CellScope is a C++20 and Python diagnostic platform for analyzing simulated cellular power, thermal, radio, wake, CPU, and battery behavior.

Use the architecture and user guides to build, run, and extend the analyzer.
